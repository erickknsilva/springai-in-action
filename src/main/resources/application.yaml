spring:
  application:
    name: board-game-buddy
  ai:
    openai:
      api-key: ${SPRING_AI_OPENAI_API_KEY}
  ai:
    openai:
      api-key: ${SPRING_AI_OPENAI_API_KEY}
  ai:
    openai:
      api-key: ${SPRING_AI_OPENAI_API_KEY}
  ai:
    openai:
      api-key: ${SPRING_AI_OPENAI_API_KEY}
  ai:
    openai:
      api-key: ${SPRING_AI_OPENAI_API_KEY}
